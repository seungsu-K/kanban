// import { getNode } from '@/lib/getNode';

// const projectList = ['project1', 'project2', 'project3'];

// const projectLists = getNode('.projectLists');

// const renderList = () => {
//   projectList.map((item) => projectLists.insertAdjacentHTML('beforeend', item));
// };

// renderList();
